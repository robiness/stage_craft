name: stage_craft
description: Put your widgets on a stage.

publish_to: 'none'

version: 0.0.5+1

environment:
  sdk: '>=2.18.0 <4.0.0'

dependencies:
  cupertino_icons: ^1.0.2
  flex_color_picker: ^3.0.2
  flutter:
    sdk: flutter
  universal_io: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^2.0.0


flutter:

  assets:
    - assets/
  uses-material-design: true
