name: example
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  stage_craft:
    path: ../
  window_manager: ^0.3.5


dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^2.0.0


flutter:

  uses-material-design: true
